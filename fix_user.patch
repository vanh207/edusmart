--- a/server/index.js
+++ b/server/index.js
@@ -4769,27 +4769,28 @@
         district,
         ward,
         school_level,
         class_name,
-        current_class_id
-      ] = Object.values(req.body);
+        current_class_id,
+      ] = [
+        req.body.full_name, req.body.email, req.body.role, req.body.grade_level, 
+        req.body.school, req.body.birth_date, req.body.specialty, req.body.qualification, 
+        req.body.gender, req.body.place_of_birth, req.body.province, req.body.district, 
+        req.body.ward, req.body.school_level, req.body.class_name, req.body.current_class_id
+      ];
 
-      db.run(
-        "UPDATE users SET full_name = ?, email = ?, role = ?, grade_level = ?, school = ?, birth_date = ?, specialty = ?, qualification = ?, gender = ?, place_of_birth = ?, province = ?, district = ?, ward = ?, school_level = ?, class_name = ?, class_id = ? WHERE id = ?",
-        [
-          full_name,
-          email,
-          role,
-          grade_level,
-          school || null,
-          birth_date || null,
-          specialty || null,
-          qualification || null,
-          gender || null,
-          place_of_birth || null,
-          province || null,
-          district || null,
-          ward || null,
-          school_level || null,
-          class_name || null,
-          current_class_id || null,
-          req.params.id
-        ],
+      const sql = "UPDATE users SET full_name = ?, email = ?, role = ?, grade_level = ?, school = ?, birth_date = ?, specialty = ?, qualification = ?, gender = ?, place_of_birth = ?, province = ?, district = ?, ward = ?, school_level = ?, class_name = ?, class_id = ?, current_class_id = ? WHERE id = ?";
+      const params = [
+        full_name,
+        email,
+        role,
+        grade_level,
+        school || null,
+        birth_date || null,
+        specialty || null,
+        qualification || null,
+        gender || null,
+        place_of_birth || null,
+        province || null,
+        district || null,
+        ward || null,
+        school_level || null,
+        class_name || null,
+        current_class_id || null,
+        current_class_id || null,
+        req.params.id
+      ];
+
+      db.run(sql, params,
         (err) => {
           if (err) {
             console.error("Lỗi khi cập nhật người dùng:", err);
